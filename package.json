{
  "name": "relay-mission-control",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Relay Mission Control for Home Assistant",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint frontend --ext .js"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "vite": "^7.0.0"
  },
  "scripts": {
  "dev": "vite",
  "build": "vite build",
  "deploy": "powershell -ExecutionPolicy Bypass -File ./scripts/deploy.ps1"
}
}